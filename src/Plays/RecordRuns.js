let score = 0;

let RecordRuns = (obj1, obj2) => {

  obj2.runs += obj1.homePlate;

  // obj2.runs =+ score;
  // console.log(score);
  // obj1.homePlate = 0;

  // if(obj2.currentSide === 'home'){
  //   return obj2.homeScore += score;
  // } else {
  //   return obj2.awayScore += score;
  // }

}

export default RecordRuns;